package koalaexample;

import java.awt.image.BufferedImage;

public class Exit extends Stationary {
    public Exit(int x, int y, boolean vis, BufferedImage image) {
        super(x, y, vis, image);
    }
}
