package koalaexample;

import java.awt.image.BufferedImage;

public class Message extends Stationary {
    public Message(int x, int y, boolean vis, BufferedImage image) {
        super(x, y, vis, image);
    }
}
